# CareGradients AI — Streamlit Secrets
# -------------------------------------------------------
# SMTP Email Configuration for OTP Registration
# -------------------------------------------------------
# To send OTP verification emails using Brevo:
#   1. Create an account on Brevo (formerly Sendinblue)
#   2. Go to SMTP & API -> SMTP
#   3. Generate a new SMTP key
#   4. Paste the generated key in `app_password` below
# -------------------------------------------------------


[email]
smtp_server = "smtp.gmail.com"
smtp_port = 587
sender_email = "dheerajtiwari.babu@gmail.com"
app_password = "kllhzwngybzpkejp"

# -------------------------------------------------------
# Legacy auth credentials (no longer used — users are now
# stored in the SQLite database). Kept for reference only.
# Default admin: username=admin, password=CareGradients@2026
# -------------------------------------------------------
